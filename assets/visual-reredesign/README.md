# Visual Re-Redesign Assets

R1 keeps reusable visual work isolated here. The current design-lab proof intentionally builds most physical artifacts with HTML/CSS so the visual language can be approved before production assets are extracted.

Any R2+ reusable marks, stamps, ticket hardware, or other production assets derived from the approved R1 language should live in this directory rather than the legacy asset folders until integration is deliberate.
